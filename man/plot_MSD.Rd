% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_function.R
\name{plot_MSD}
\alias{plot_MSD}
\title{Plot Multidimensional Scaling (MDS) of Methylation Data}
\usage{
plot_MSD(
  df,
  metadata,
  grouping_variable,
  top_plot = 1000,
  save_file,
  width = 20,
  height = 10
)
}
\arguments{
\item{df}{A data frame or matrix containing methylation values or similar numeric data.}

\item{metadata}{A data frame containing sample metadata with columns matching sample IDs.}

\item{grouping_variable}{A character vector of one or more column names in `metadata` to group/color samples.}

\item{top_plot}{Integer, number of top features (rows) to use for MDS calculation (default 1000).}

\item{save_file}{Character string file path to save the plot (optional). If missing or NULL, plot is displayed.}

\item{width}{Numeric, width of saved plot in inches (default 20).}

\item{height}{Numeric, height of saved plot in inches (default 10).}
}
\value{
NULL. Displays the MDS plot, and optionally saves it to `save_file`.
}
\description{
This function creates an MDS plot (using `plotMDS`) from methylation data (`df`) 
colored by levels of a grouping variable in the metadata. Optionally saves the plot to a file.
}
\examples{
\dontrun{
plot_MSD(df = methylation_matrix, metadata = sample_metadata, grouping_variable = "Treatment", top_plot = 500)
plot_MSD(df = methylation_matrix, metadata = sample_metadata, grouping_variable = c("Treatment", "Batch"), save_file = "MDSplot.png")
}
}
