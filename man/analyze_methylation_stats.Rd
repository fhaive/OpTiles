% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_methylation_stats.R
\name{analyze_methylation_stats}
\alias{analyze_methylation_stats}
\title{Analyze and Plot Distribution of Coverage or Beta Values from Methylation Data}
\usage{
analyze_methylation_stats(
  methdata_df,
  variable_to_investigate,
  metadata = NULL,
  sample_name_variable = NULL,
  position_text = 50
)
}
\arguments{
\item{methdata_df}{A data frame containing methylation data.
- If `variable_to_investigate = "coverage"`, it should contain columns with "coverage" in their names.
- If `variable_to_investigate = "betavalue"`, columns should correspond to sample names from metadata.}

\item{variable_to_investigate}{A character string specifying which variable to analyze: either `"coverage"` or `"betavalue"`.}

\item{metadata}{Either a data frame or character vector containing sample metadata.
Required if `variable_to_investigate = "betavalue"`.}

\item{sample_name_variable}{Character string specifying the column name in `metadata` that contains sample names.
Required if `variable_to_investigate = "betavalue"`.}

\item{position_text}{Numeric scalar indicating the X-axis position where statistics text will be placed in the plot. Default is 50.}
}
\value{
No return value. The function generates and displays a histogram plot of the selected variable distribution.
}
\description{
This function computes and plots the distribution of either read coverage or beta values (percent methylation) 
from methylation data. It validates inputs and requires that, when investigating beta values, 
the input data frame columns match sample names provided in the metadata.
}
\details{
When `variable_to_investigate` is `"coverage"`, the function extracts coverage columns from `methdata_df` 
and plots their distribution.  
When `variable_to_investigate` is `"betavalue"`, it checks that `methdata_df` columns match sample names in `metadata` 
(using `sample_name_variable`), then plots the beta values distribution.
}
