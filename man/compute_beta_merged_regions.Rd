% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handling_consecutive_regions.R
\name{compute_beta_merged_regions}
\alias{compute_beta_merged_regions}
\title{Compute Mean or Median Beta Values for Merged CpG Regions}
\usage{
compute_beta_merged_regions(regions_cpgs, method, metadata, sample_name)
}
\arguments{
\item{regions_cpgs}{A \code{data.frame} or \code{data.table} containing beta values for CpG sites. Must include
a \code{region_id} column, a \code{cpg_id} column, and one or more columns with beta values per replicate.}

\item{method}{A character string specifying the summary method. Either \code{"mean"} or \code{"median"}.}

\item{metadata}{A \code{data.frame} containing sample annotations. Must include a column matching the sample names
used in \code{regions_cpgs}, and another column with group identifiers.}

\item{sample_name}{A character string indicating the name of the column in \code{metadata} that contains sample labels}
}
\value{
A \code{data.table} summarizing the beta values for each \code{region_id} and each group. Each cell contains
  the mean or median beta value for that group in that region.
}
\description{
This function summarizes beta values across CpG sites within each region by computing either the mean or median.
Replicate-level beta values are grouped using metadata, and summaries are computed per region for each defined group.
}
\examples{
\dontrun{
df <- data.frame(
  region_id = c("region1", "region1", "region2"),
  cpg_id = c("cg001", "cg002", "cg003"),
  T0_24_1 = c(0.5, 0.6, 0.7),
  T0_24_2 = c(0.4, 0.7, 0.8),
  T0_48_1 = c(0.6, 0.5, 0.6)
)
compute_beta_merged_regions(df, method = "mean")
}

}
